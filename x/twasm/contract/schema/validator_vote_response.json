{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ValidatorVoteResponse",
  "type": "object",
  "required": [
    "votes"
  ],
  "properties": {
    "votes": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/ValidatorVote"
      }
    }
  },
  "definitions": {
    "Binary": {
      "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>",
      "type": "string"
    },
    "ValidatorVote": {
      "description": "This is taken from BeginBlock.LastCommitInfo See https://github.com/tendermint/tendermint/blob/v0.34.8/proto/tendermint/abci/types.proto#L348-L352",
      "type": "object",
      "required": [
        "address",
        "power",
        "voted"
      ],
      "properties": {
        "address": {
          "$ref": "#/definitions/Binary"
        },
        "power": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "voted": {
          "type": "boolean"
        }
      }
    }
  }
}
